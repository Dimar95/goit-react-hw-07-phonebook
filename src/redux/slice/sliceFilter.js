// import { createSlice } from '@reduxjs/toolkit';
// import { useSelector } from 'react-redux';
// const tasksInitialState = useSelector(state => state.contact)
// export const filterContact = createSlice({

//   name: 'filter',
//   initialState: state.contact,
//   reducers: {
//     filterContacts(state, action) {
//         console.log("🚀 ~ action:", action);
      
//         return state.filter(contact =>
//             contact.toLowerCase().includes(action.payload.toLowerCase()))

//     }
//   },
// });

// export const { filterContacts } = filterContact.actions;
